replicaCount: 1

image:
  repository: nimbus/dashboard-ui
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 80
  targetPort: 80

livenessProbe:
  path: /
  port: 80

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
