apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dashboard-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dxrshn/nimbus.git
    targetRevision: main
    path: apps/dashboard-ui/helm
    helm:
      valueFiles:
        - values-local.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: nimbus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
